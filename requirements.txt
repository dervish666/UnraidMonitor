# Docker production requirements — superset of pyproject.toml [dependencies] + [ai].
# AI packages (anthropic, openai) are optional in pyproject.toml but required in
# the Docker image so all features work out of the box.
docker>=7.0.0,<8.0.0
aiogram>=3.4.0,<4.0.0
aiohttp>=3.13.5,<4.0.0
pyyaml>=6.0,<7.0
pydantic>=2.0,<3.0
pydantic-settings>=2.0,<3.0
anthropic>=0.40.0,<1.0.0
openai>=2.0.0,<3.0.0
psutil>=6.1.0,<8.0.0
python-dotenv>=1.0.0,<2.0.0

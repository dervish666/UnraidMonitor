# Implementation Plan — Codebase Audit 2026-06-06 15:38

## Overview
15 findings (1 High, 5 Medium, 9 Low) — all in code added since the May 14 audit, plus 2 re-filed partial carryovers. Estimated total effort: ~1 day. No Critical items; H1 is the only behavioural bug with real user impact.

## Phase 1: Critical & Security (Do First)

| # | Finding | Files | Effort | Description |
|---|---------|-------|--------|-------------|
| 1 | ✅ DONE — H1 — Auto-heal never retries stuck-unhealthy containers | `src/monitors/docker_events.py:558-570`, `src/services/auto_healer.py:45-64` | M | Make `heal()` return a status (healed / failed / gave_up). In `_handle_health_event`, after the heal call, `self._unhealthy_alerted.discard(container_name)` unless gave_up — so the next unhealthy event re-enters heal and the storm guard provides the bound. In `heal()`, capture `result = await self._controller.restart(...)`; if `result.startswith("❌")`, send a failure alert instead of the success-implying "attempt N/M". Add tests: (a) unhealthy→heal→unhealthy with no healthy in between triggers a second heal; (b) attempt max_restarts+1 sends the gave-up escalation exactly once; (c) failed restart sends a failure alert. |
| 2 | ✅ DONE — M1 — Unvalidated container names persisted via auto-heal picker | `src/bot/manage_command.py:662-670`, `src/config.py:395-403` | S | In `feat_heal_toggle_callback`, after extracting `container`, reject names failing `validate_container_name()` (import from `src/utils/formatting.py`) — better, reject anything not in `_heal_candidates(state, protected_containers)`. In `AutoHealConfig.set_containers()`, filter the list through `validate_container_name` defensively. Add a spoofed-callback test. |
| 3 | ✅ DONE — M3 — Unredacted Unraid error bodies | `src/unraid/client.py:195-209` | S | Add `def _safe_body(self, text: str) -> str: text = text[:200]; return text.replace(self._api_key, "[REDACTED]") if self._api_key else text`. Use in the non-200 raise, the JSONDecodeError raise, and keep `connect()` using it too. Third-cycle carryover — close it out. |

## Phase 2: High Priority Bugs & Data Integrity

| # | Finding | Files | Effort | Description |
|---|---------|-------|--------|-------------|
| 4 | ✅ DONE — M2 — `gather_context` crashes after "Analyzing…" | `src/services/diagnostic.py:124-131` | S | Broaden the `containers.get` except to `docker.errors.DockerException` → return None. Wrap the `container.logs` call in try/except → `logs = "(logs unavailable)"` and continue gathering the rest of the context. Test: logs raising APIError still yields a DiagnosticContext. |
| 5 | ✅ DONE — M4 — Image-update dedup lost on every restart | `src/monitors/image_update_monitor.py:45,97-99` | M | Persist `_notified` to `data/announced_updates.json` (atomic mkstemp+os.replace, mirroring `version_store.py`); load in `__init__`, save after each `check_once()` that produced updates. While in `check_once()`, drop `_notified` entries whose container no longer exists. Tests: restart (new instance, same file) does not re-announce; corrupted file degrades to empty dict. |

## Phase 3: Performance & Scalability
None this cycle — the perf sweep's Critical claims were struck on verification (all new structures are bounded). The two genuine residues (orphaned `_restarts`/`_notified` entries for removed containers) are folded into items 5 and 8.

## Phase 4: Code Quality & Tech Debt

| # | Finding | Files | Effort | Description |
|---|---------|-------|--------|-------------|
| 6 | ✅ DONE — M5 — Callback parsing boilerplate ×10 | `src/bot/alert_callbacks.py:46-71,106-139,219-238,303-319,361-371,421-431,465-475,594-614,674-695,734-741` | L | Extract `parse_container_callback(callback, state)` (simple `prefix:name`) and `parse_valued_callback(callback, state)` (`prefix:name:value` rsplit variant); both answer the error themselves and return None on failure. Replace all 10 sites. Run the full alert-callback test files after each batch of replacements. |
| 7 | ✅ DONE — L1 — Wall-clock storm-guard window | `src/services/auto_healer.py:37-46` | S | Switch `_restarts` to `list[float]` of `time.monotonic()`; adjust `_recent_count` cutoff arithmetic to seconds. Do together with item 1 since both touch `heal()`. |
| 8 | ✅ DONE — L4 — `_violation_last_seen` lazy init | `src/monitors/resource_monitor.py:393-394` | S | Move `self._violation_last_seen: dict[str, float] = {}` to `__init__`; delete the `hasattr` guard. Optionally pop last-seen entries when evicting. |
| 9 | ✅ DONE — L6 — `_recent_count` side effects | `src/services/auto_healer.py:37-43` | S | Rename to `_prune_and_count()` (or document the mutation). Fold into item 1's PR. |
| 10 | ✅ DONE — L5 — `extract_local_digests(container: Any)` | `src/monitors/image_update_monitor.py:13` | S | Type the param via `if TYPE_CHECKING: from docker.models.containers import Container` and use `"Container"`. |
| 11 | ✅ DONE — L7 — `atomic_yaml_write` file mode | `src/config.py:56-71` | S | Add `os.fchmod(fd, 0o644)` after mkstemp, matching `version_store.py`. |

## Phase 5: Nice-to-Haves & Hardening

| # | Finding | Files | Effort | Description |
|---|---------|-------|--------|-------------|
| 12 | ✅ DONE — L2 — Duplicate restart broadcasts | `src/restart.py:50-57` | S | Module-level `_restart_in_progress` bool; log-and-return if already set. |
| 13 | ✅ DONE — L3 — Corrupted version store re-shows What's new | `src/utils/version_store.py:20-21` | S | On `JSONDecodeError`, log a warning and `os.remove(path)` (best-effort) before returning None. |
| 14 | ✅ DONE — L8 — `telegram_format.py` missing from structure YAML | `.claude/structure/utils.yaml` | S | Add the module entry (exports: `markdown_to_telegram_html`, `strip_html_tags`) per CLAUDE.md's own rule. |
| 15 | ✅ DONE — L9 — No CVE scanning | `pyproject.toml`, `.github/workflows/` | S | Add `pip-audit` to dev extras and a CI step (`uv run pip-audit` or `uv run --with pip-audit pip-audit`). |
| 16 | ✅ DONE — Hardening — defense-in-depth (not findings) | `src/services/diagnostic.py:266`, `tests/test_telegram_format.py` | S | Wrap `context.alert_context` in `sanitize_for_prompt()`; add a regression test asserting model output containing `<tg-spoiler>`/`<a href>` arrives escaped. |

## Dependencies & Ordering Notes
- Items 1, 7, 9 all touch `auto_healer.py` — do as one PR (auto-heal correctness).
- Items 5 and 10 both touch `image_update_monitor.py` — one PR.
- Item 6 (parse helpers) should land **before** any new callback handlers are written; it structurally prevents the next item-2-class gap.
- Everything else is independent and parallelisable.

## Quick Wins
Items 2, 3, 4, 8, 11, 13, 14 are all S-effort; together under an hour. Item 3 closes the oldest open carryover (L8, since April).

## Progress Log

- **2026-06-06 (same day as audit)**: Items 1-5, 7-16 completed — all of Phases 1, 2, 5 and everything in Phase 4 except item 6. Item 6 (M5) completed same day — ALL 16 plan items done.
- First pip-audit run found 6 CVEs: idna, requests, urllib3 upgraded in uv.lock; **aiohttp 3.13.5 (CVE-2026-34993, CVE-2026-47265, fixed in 3.14.0) is pinned by aiogram 3.28.2 (`aiohttp<3.14`)** — re-check when aiogram releases. Exposure is client-only (no aiohttp server in this bot).

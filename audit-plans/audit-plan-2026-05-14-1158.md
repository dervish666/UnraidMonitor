# Implementation Plan — Codebase Audit 2026-05-14 11:58

## Overview

29 findings total (0 Critical, 2 High, 14 Medium, 13 Low). 12 carried over from prior audit (Apr 23), 17 new. Estimated effort: ~14 S, ~10 M, ~5 L tasks. The 6 quick wins (items 3-8 below) can be completed in under an hour.

## Phase 1: Security & Race Conditions (Do First)

| # | Finding | Files | Effort | Description |
|---|---------|-------|--------|-------------|
| 1 | H1 — Unsanitized tool results fed to LLM | `src/services/nl_processor.py:293`, `src/services/nl_tools.py:464` | M | In `nl_processor.py:293`, wrap tool result: `result = sanitize_for_prompt(result, max_length=8000)` before appending to `tool_results`. In `nl_tools.py:464`, sanitize each error string: `error_display = sanitize_for_prompt(error[:200])`. Import `from src.utils.sanitize import sanitize_for_prompt` in both files. |
| 2 | H2 — MemoryMonitor kill/decline race | `src/monitors/memory_monitor.py:162-214` | M | Add `self._kill_lock = asyncio.Lock()` to `__init__`. Wrap `_execute_kill_countdown()` body in `async with self._kill_lock`. Also wrap `cancel_pending_kill()`, `confirm_restart()`, and `decline_restart()` in the same lock. Re-check `_kill_cancel_event.is_set()` immediately before `_stop_container()` at line 196. Add a test that verifies cancel after countdown passes the event check. |
| 3 | M3 — World-writable ignore files | `src/alerts/ignore_manager.py:359` | S | Change `os.fchmod(fd, 0o666)` to `os.fchmod(fd, 0o644)`. |
| 4 | M2 — Uncapped mute duration | `src/bot/alert_callbacks.py:277, 629, 802` | S | After `minutes = int(parts[1])`, add `minutes = max(1, min(minutes, 43200))` in all three locations (container mute, array mute, server mute). |
| 5 | M10 — SSRF via setup wizard | `src/bot/setup_wizard.py:617-619` | M | Add a `_validate_host(host: str) -> bool` function that rejects RFC 1918 addresses (10.x, 172.16-31.x, 192.168.x), link-local (169.254.x), localhost, and `0.0.0.0`. Call before `test_unraid_connection()`. Warn user but allow override for LAN setups (since Unraid IS typically on a private IP — validate it's not metadata/loopback specifically). |
| 6 | L8 — API key logged on connection failure | `src/unraid/client.py:136` | S | Replace `text` with `text[:200].replace(self._api_key, "[REDACTED]")` in the log line. |

## Phase 2: Data Integrity & Accuracy

| # | Finding | Files | Effort | Description |
|---|---------|-------|--------|-------------|
| 7 | M1 — ChatIdStore non-atomic writes | `src/alerts/manager.py:84-92` | M | Replace bare `json.dump()` with tempfile + `os.replace()` pattern. Use `tempfile.mkstemp(dir=parent, prefix=".tmp_chatids_", suffix=".json")`, write, `os.fchmod(fd, 0o644)`, `os.replace()`. Match `base_mute_manager.py:149-159`. |
| 8 | M4 — BOT_VERSION stale | `src/bot/health_command.py:23` | S | Replace `BOT_VERSION = "0.9.2"` with: `from importlib.metadata import version, PackageNotFoundError; try: BOT_VERSION = version("unraid-monitor-bot"); except PackageNotFoundError: BOT_VERSION = "unknown"`. |
| 9 | L4 — parse_duration unbounded | `src/alerts/mute_manager.py:14` | S | After `if value <= 0: return None`, add `if value > 10080: return None` (7-day cap). |

## Phase 3: Performance & Memory

| # | Finding | Files | Effort | Description |
|---|---------|-------|--------|-------------|
| 10 | M7 — Provider re-instantiated every call | `src/services/llm/registry.py:141-162, 365-388` | M | Add `self._provider_cache: dict[tuple[str, str], LLMProvider] = {}` to `__init__`. In `get_provider`, check cache keyed on `(provider_name, model_name)` before calling `_create_provider`. Invalidate cache in `set_model()` and `set_feature_model()`. Also build `self._ollama_id_map: dict[str, bool]` (model_id → supports_tools) once in init instead of scanning `_ollama_models` linearly. |
| 11 | M5 — CrashTracker unbounded keys | `src/monitors/docker_events.py:36-103` | M | Add `_cleanup_stale(self)`: remove entries from `_crashes` where list is empty, remove `_last_escalation`/`_last_recovery_alert` entries older than 24h. Call from `record_crash()` every 100 invocations (counter + modulo pattern). |
| 12 | M6 — Pattern cache unbounded | `src/monitors/log_watcher.py:26-27` | S | Replace mutable default with module-level `_PATTERN_CACHE: dict = {}` and `_PATTERN_CACHE_MAX = 64`. Check `if len(_PATTERN_CACHE) > _PATTERN_CACHE_MAX: _PATTERN_CACHE.clear()` before caching. |
| 13 | M11 — _unhealthy_alerted never pruned | `src/monitors/docker_events.py:175` | S | In `_reconnect()` at line 257, add `self._unhealthy_alerted.clear()`. Containers will be re-detected on the next health check event. |
| 14 | L5 — NL processor message list copying | `src/services/nl_processor.py:299` | S | Change `messages = messages + [...]` to `messages.extend([...])`. |
| 15 | L9 — Sequential startup | `src/startup.py:240-263` | M | Wrap Anthropic model discovery, Ollama model discovery, and aiohttp session creation in `asyncio.gather()`. Handle individual failures with `return_exceptions=True`. |
| 16 | L11 — Thread pool exhaustion | `src/monitors/log_watcher.py:126` | S | Add a dedicated `ThreadPoolExecutor(max_workers=50)` to LogWatcher and use `loop.run_in_executor(self._pool, ...)` instead of `asyncio.to_thread`. |

## Phase 4: Architecture & Code Quality

| # | Finding | Files | Effort | Description |
|---|---------|-------|--------|-------------|
| 17 | M9 — alert_callbacks.py DRY violations | `src/bot/alert_callbacks.py`, `src/utils/formatting.py` | L | (a) Extract `format_duration_minutes(minutes: int) -> str` to `formatting.py`. Replace 3 copy-pasted blocks at lines 307-312, 635-640, 807-812. (b) Extract `parse_container_callback(callback, state) -> tuple[str, CallbackQuery] | None` helper that handles the repeated null-check/split/validate/find/error preamble. (c) Extract generic `threshold_picker_callback(config, metric_map, steps, prefix)` factory to deduplicate the three threshold handlers. |
| 18 | M8 — register_commands god function | `src/bot/telegram_bot.py:129-493` | L | Split into `_register_docker_commands(dp, state, controller, ...)`, `_register_unraid_commands(dp, state, ...)`, `_register_memory_commands(dp, ...)`, `_register_nl_handler(dp, ...)`. Type all parameters against their actual classes (not `Any`). |
| 19 | M13 — start_monitoring god function | `src/startup.py:224-444` | L | Extract into `_init_docker_monitors(config, ...)`, `_init_unraid_monitors(config, ...)`, `_init_nl_processor(config, ...)`. Each returns its initialized components. `start_monitoring` becomes an orchestrator calling these. |
| 20 | M12 — Private attribute mutation | `src/startup.py:389` | S | Add `def set_controller(self, controller: ContainerController)` to `NLProcessor` that calls `self._executor.set_controller(controller)`. Add `set_controller` to `NLToolExecutor`. Replace `nl_processor._executor._controller = controller` at startup.py:389. |
| 21 | M14 — Triplicated YAML persist | `src/config.py:258-289, 396-428`, `src/services/llm/registry.py:454-503` | M | Extract `persist_yaml_section(path: Path, section: str, updater: Callable)` to `src/utils/persistence.py`. Handles: read YAML, call updater on section dict, atomic write via tempfile. Replace all three call sites. |
| 22 | L6 — Module-level mutable _MODEL_FAMILIES | `src/services/llm/registry.py:23-27` | S | Copy `_MODEL_FAMILIES` in `ProviderRegistry.__init__`: `self._model_families = dict(_MODEL_FAMILIES)`. Update `_update_families_from_discovered` and `_resolve_model` to use instance copy. |
| 23 | L7 — Threshold constants scattered | `src/bot/alert_callbacks.py:464-469` | S | Move `CPU_THRESHOLD_STEPS`, `MEMORY_THRESHOLD_STEPS`, etc. to `src/constants.py`. |

## Phase 5: Cleanup & DX

| # | Finding | Files | Effort | Description |
|---|---------|-------|--------|-------------|
| 24 | L1 — Redundant asyncio imports | `src/monitors/resource_monitor.py:178, 205` | S | Delete `import asyncio` at lines 178 and 205. |
| 25 | L2 — Redundant time import | `src/monitors/log_watcher.py:217` | S | Delete `import time` at line 217. |
| 26 | L3 — Health command encapsulation | `src/bot/health_command.py`, `src/monitors/*.py` | L | Add public status properties: `DockerEventMonitor.is_running`, `LogWatcher.is_running`, `LogWatcher.total_drops`, `ResourceMonitor.is_running`, `MemoryMonitor.is_running`. Update health_command.py to use these. |
| 27 | L10 — requirements.txt/pyproject.toml gap | `requirements.txt`, `pyproject.toml` | S | Add comment to `requirements.txt` explaining it's the Docker-specific superset that includes AI deps. Or consolidate by using `pip install .[ai]` in the Dockerfile. |
| 28 | L12 — AI tests skip in dev env | `pyproject.toml`, `tests/test_api_errors.py` | S | Add `anthropic` and `openai` to `[project.optional-dependencies] dev`. Or refactor tests to mock the imports so they run without the actual packages. |
| 29 | L13 — No pytest-cov | `pyproject.toml` | S | Add `"pytest-cov>=4.0"` to `[project.optional-dependencies] dev`. |

## Phase 6: Process Improvements

| # | Recommendation | Effort | Description |
|---|----------------|--------|-------------|
| P1 | Automate version management | M | Adopt `importlib.metadata.version()` (Phase 2 item 8), add pre-commit hook: `grep -q $(python -c "from importlib.metadata import version; print(version('unraid-monitor-bot'))") src/bot/health_command.py`. Consider `python-semantic-release` for automated versioning. |
| P2 | Track audit findings as issues | S | After each audit, create GitHub Issues for all High/Medium findings with "tech-debt" label. Review at sprint planning. |
| P3 | Shared test helpers | M | Create `tests/helpers.py` with `make_mock_message()`, `make_mock_callback()`, `make_mock_state()` used across 75 test files. Does not violate the "no conftest.py" principle — helpers are explicit imports, not magic. |
| P4 | Docker build optimization | S | Change Dockerfile to `COPY pyproject.toml uv.lock . && uv sync` before `COPY src/`, so dependency installs are cached across source changes. |
| P5 | Duplication detection in CI | S | Add `pylint --disable=all --enable=duplicate-code src/` or `jscpd` to CI pipeline. |
| P6 | AI deps in dev environment | S | `uv pip install -e ".[ai,dev]"` in dev setup instructions. Eliminates the 8 skipped tests. |

## Dependencies & Ordering Notes

- **H1 and H2 are independent** — can be done in parallel. Both should be first priority.
- **Items 3-6 (M3, M2, M10, L8)** are all independent quick security fixes.
- **Item 10 (provider caching)** should come before Phase 4 architecture work since registry.py is involved.
- **Item 17 (DRY extraction)** should come before item 18 (register_commands split) since extracted helpers need to be registered.
- **Items 24-29** (Phase 5) are all independent and can be batched into a single commit.
- **P1 depends on item 8** (BOT_VERSION fix) being done first.

## Quick Wins (S effort + High/Medium severity)

Do these first — under 30 minutes total:
1. **M3** — `0o666` → `0o644` (one character change)
2. **M2** — Cap mute duration (one line × 3 locations)
3. **M4** — Fix BOT_VERSION (two lines)
4. **M6** — Bound pattern cache (five lines)
5. **M11** — Clear `_unhealthy_alerted` on reconnect (one line)
6. **M12** — Add `set_controller()` method (three line change + one line at call site)
